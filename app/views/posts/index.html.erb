<%= stylesheet_link_tag 'application', 'index' %>

<div class="container">
  <% @posts.each do |post| %>
    <h2> <%= post.title %> </h2>
    <p> <%= post.content %> </p>
  <% end %>

  <ul>
    <% @posts.each do |post| %>
    <li><%= link_to post.title, post %></li>
    <% end %>
  </ul>
</div>