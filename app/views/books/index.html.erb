<h1>Books</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= book.title %> by <%= book.author %>
      (<%= book.rating %>/5)
      <%= link_to 'Read more', book %>
    </li>
  <% end %>
</ul>